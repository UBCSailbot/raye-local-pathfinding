import math
import numpy as np
from updated_geometric_planner import Obstacle
from matplotlib import pyplot as plt
from matplotlib import patches

axes = plt.gca()
axes.add_patch(patches.Wedge((0, 0), 1, 0, 30))
plt.show()
